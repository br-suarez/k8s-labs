apiVersion: v1
kind: LimitRange
metadata:
  name: practice-app-limits
  namespace: practice-app
spec:
  limits:
    - type: Container
      default:
        cpu: "100m"
        memory: "128Mi"
      defaultRequest:
        cpu: "50m"
        memory: "64Mi"
