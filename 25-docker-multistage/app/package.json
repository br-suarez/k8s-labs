{
  "name": "checkout-api",
  "version": "1.0.0",
  "private": true,
  "description": "Small HTTP service used to compare naive and multi-stage image builds.",
  "main": "src/server.js",
  "scripts": {
    "build": "esbuild src/server.js --bundle --platform=node --target=node22 --outfile=dist/server.js",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "express": "4.21.2"
  },
  "devDependencies": {
    "esbuild": "0.24.2"
  }
}
