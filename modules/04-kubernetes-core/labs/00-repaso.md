# Lab 04.00 — Repaso

**CORE · 15 min.** En frío, antes del diagnóstico.

Del módulo 03:

1. ¿Por qué un binario Go estático en `distroless/static` no puede hacer
   peticiones HTTPS? Una frase, y el arreglo.
2. `ENTRYPOINT app` vs `ENTRYPOINT ["app"]`. ¿Cuál tarda 10 s en parar y por qué?
3. `docker pull app:v1` en dos máquinas da digests distintos. ¿Posible?

Del módulo 02:

4. Escribe de memoria el `proxy_cache_key` que provocó la fuga entre tenants, y
   di qué le faltaba.

Lo que falles → `NOTAS.md`, repaso a 30 días.
