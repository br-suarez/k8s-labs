# Lab 05.00 — Repaso

**CORE · 15 min.** En frío, antes del diagnóstico.

Del módulo 04:

1. Las tres probes: qué pregunta responde cada una y qué pasa cuando falla.
2. ¿Por qué un `preStop: sleep 5` reduce los 502 durante un rollout si la app ya
   maneja `SIGTERM` bien?
3. Un pod está `Running` pero no aparece en el EndpointSlice. Dos causas.

Del módulo 02:

4. Escribe de memoria el bloque `location` que enruta `/api/` a un upstream
   preservando el prefijo. En el módulo 05 vas a borrar esto — conviene saber
   qué estás sustituyendo.

Lo que falles → `NOTAS.md`, repaso a 30 días.
