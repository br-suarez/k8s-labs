# Lab 03.00 — Repaso

**CORE · 15 min.** En frío, antes del diagnóstico.

Del módulo 02:

1. `$request_time` alto y `$upstream_response_time` bajo. ¿De quién es el
   problema? Una frase.
2. Escribe de memoria las dos directivas que hacen que `keepalive` al upstream
   funcione de verdad.
3. ¿Qué contiene una clave de caché para contenido autenticado, y por qué la
   respuesta correcta suele ser "no lo caches aquí"?

Del módulo 01:

4. Nombra dos casos en los que `set -e` no aborta el script.

Lo que falles va a `NOTAS.md` → repaso a 30 días.
