# Lab 13.00 — Repaso

**CORE · 15 min.** En frío, antes del diagnóstico.

Del módulo 12:

1. Una política de admisión en `Enforce` y una imagen sin firmar corriendo.
   ¿Contradicción? Una frase.

Del módulo 09:

2. ¿Qué hace `concurrency` en un workflow y por qué `cancel-in-progress` debe
   ser distinto en `main` que en un PR? Hoy vas a necesitar el mismo mecanismo,
   con una diferencia importante.

Del módulo 03:

3. ¿Por qué un tag de imagen no es una identidad? La respuesta se aplica igual a
   las versiones de módulos y proveedores.

Lo que falles → `NOTAS.md`, repaso a 30 días.
